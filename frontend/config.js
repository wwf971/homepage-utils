// Default configuration for Spring Learn frontend
// This file is committed to git and contains default/example values
// For real/private config, create config.0.js (which is gitignored and has higher priority)

// Configuration Priority: localStorage > config.0.js > config.js > default
// 1. localStorage: User-set values in the browser (highest priority)
// 2. config.0.js: Private/local configuration file (gitignored)
// 3. config.js: Default configuration file (committed to git)
// 4. default: Hardcoded fallback value in the application

// Backend server URL
// In development, this might point to localhost
// In production, this should point to your actual backend server
SERVER_URL = 'http://localhost:900';
